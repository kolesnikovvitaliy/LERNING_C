# 🚀 Advanced ANSI C Color Logger


Компактная, быстрая и гибкая система логирования для проектов на языке **C**. Модуль поддерживает цветовую разметку терминала с помощью ANSI-последовательностей, фильтрацию сообщений по уровням важности (verbosity) и динамическое управление конфигурацией во время выполнения.

---

## 📊 Архитектура и логика работы (Mermaid)

Диаграмма ниже демонстрирует полный цикл обработки входящего сообщения функцией `log_message()`: от фильтрации по маске до финального сброса цвета в терминале.

```mermaid
graph TD
    A([Начало: вызов log_message]) --> B{Level > current_log_level?}

    %% Ветка фильтрации
    B -- Да (Игнорировать) --> C([Выход из функции])
    B -- Нет (Обработать) --> D[Получение времени через time и localtime]

    %% Ветка форматирования времени
    D --> E[Форматирование времени в строку HH:MM:SS]
    E --> F{"Проверка: level == 0 (NONE)?"}

    %% Ветка вывода префиксов
    F -- Да --> G[Переход к обработке текста]
    F -- Нет --> H{"Проверка: level == 1 (PRINT_RESULT)?"}

    H -- Да --> I["Печать только ANSI цвета"]
    H -- Нет --> J["Печать переноса строки, ANSI цвета и префикса [LEVEL]:"]

    I --> G
    J --> G

    %% Вывод текста и финализация
    G --> K["Инициализация va_list через va_start"]
    K --> L["Вывод текста пользователя через vprintf"]
    L --> M["Завершение работы с аргументами va_end"]
    M --> N["Сброс цвета терминала (CLR_RESET)"]
    N --> O(["Конец: успешный возврат"])

    %% Стилизация блоков
    style A fill:#4CAF50,stroke:#388E3C,stroke-width:2px,color:#fff
    style C fill:#f44336,stroke:#d32f2f,stroke-width:2px,color:#fff
    style O fill:#4CAF50,stroke:#388E3C,stroke-width:2px,color:#fff
    style B fill:#FF9800,stroke:#F57C00,stroke-width:2px,color:#fff
    style F fill:#FF9800,stroke:#F57C00,stroke-width:2px,color:#fff
    style H fill:#FF9800,stroke:#F57C00,stroke-width:2px,color:#fff
```

---

## 🎨 Поддерживаемые уровни и цвета

Логгер поддерживает 7 уровней важности. Каждый уровень обладает своим строковым представлением и цветовой палитрой для удобного визуального анализа логов:

| Индекс | Уровень лога | Назначение | Цвет в консоли |
| :---: | :--- | :--- | :--- |
| **0** | `NONE_LOG` | Системный уровень (сообщения игнорируются) | По умолчанию |
| **1** | `PRINT_RESULT` | Вывод чистых результатов без префиксов | Заданный макросом |
| **2** | `ERROR_LOG` | Критические ошибки и сбои приложения | 🔥 Красный |
| **3** | `INFO_LOG` | Информационные сообщения о штатной работе | 🍏 Зеленый |
| **4** | `WARNING_LOG` | Предупреждения о потенциальных проблемах | ⚠️ Желтый |
| **5** | `TRACE_LOG` | Глубокое пошаговое трассирование | 🔍 Синий / Голубой |
| **6** | `DEBUG_LOG` | Отладочная информация для разработчиков | ⚙️ Серый / Фиолетовый |

---

## 🛠️ API Справочник

### 1. Вывод лога
```c
void log_message(log_level_t level, const char *fmt, ...);
```
Главная функция системы. Принимает уровень важности, строку форматирования и вариативный список аргументов.
* **Безопасность:** Потокобезопасность зависит от реализации `vfprintf` в стандартной библиотеке вашей ОС.
* **Особенность:** Автоматически форматирует вывод под текущую конфигурацию терминала.

### 2. Изменение уровня "на лету"
```c
log_level_t set_log_level(log_level_t level);
```
Динамически изменяет текущий глобальный порог `current_log_level`.
* **Возвращает:** Предыдущее значение уровня (удобно для временного переключения режимов).

---

## 💻 Примеры интеграции и использования

### Базовый пример использования

```c
#include "my_logger.h"

int main() {
    // По умолчанию установлен уровень DEBUG — видны все логи
    INFO_LOG("Запуск системы инициализации...","");
    WARNING_LOG("Низкий уровень свободной памяти: %d МБ", 128);
    ERROR_LOG("Не удалось подключиться к серверу %s на порту %d", "127.0.0.1", 8080);

    // Вывод чистых данных (например, таблицы)
    PRINT_RESULT("Результат вычислений: X = %.2f\n", 42.42);

    return 0;
}
```

### Временное переключение уровня (Паттерн "Scoped Log")

Если вам нужно заглушить лишние логи или, наоборот, детально исследовать конкретный участок кода:

```c
void critical_section() {
    // Временно включаем режим ERROR, чтобы скрыть INFO/DEBUG сообщения
    log_level_t previous_level = set_log_level(ERROR_LOG);

    // ... этот код производит много логов, но мы увидим только ошибки ...
    INFO_LOG("Этот лог не напечатается", "");
    ERROR_LOG("А этот лог напечатается, если возникнет сбой!", "");

    // Восстанавливаем исходный уровень логгера
    set_log_level(previous_level);
}
```

---

## ⚡ Особенности реализации и Warnings

> ⚠️ **ВАЖНОЕ ПРЕДУПРЕЖДЕНИЕ**
>
> 1. **Перенос строки (`\n`):** Функция `log_message` автоматически добавляет символ новой строки `\n` перед префиксом для уровней `ERROR_LOG` и выше. Однако для текста сообщения пользователя символ `\n` должен передаваться внутри строки форматирования `fmt`.
> 2. **Сброс цвета:** В конце каждой итерации функция выполняет принудительный сброс цвета через макрос `CLR_RESET`. Убедитесь, что этот макрос корректно определен в вашем заголовочном файле `my_logger.h`.
> 3. **Формат [ДАТА] [ВРЕМЯ] [ФАЙЛ:СТРОКА] [ФУНКЦИЯ] -> СООБЩЕНИЕ:** Форматирование логов по умолчанию.
> 3. **Логирование полностью отключается на этапе компиляции, если не задан флаг -DDEBUG.** Нулевой оверхед (code footprint) в релизной сборке. Отсутствие вызовов функций логирования в скомпилированном бинарнике

---
[← Назад](https://github.com/kolesnikovvitaliy/LERNING_C/tree/main/EXTREME_C/Основные_возможноти_языка/Директивы_препроцессора/Макросы/examples/Определение_и_использование_вариативного_макроса/LAB)
---
