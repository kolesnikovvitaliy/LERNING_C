/**
 * @file main.c
 * @brief Программа демонстрации работы макросов итерации и вывода.
 *
 * Этот файл содержит классический пример того, как с помощью макропроцессора
 * языка Си можно изменить привычный синтаксис управления потоком выполнения
 * и вывода данных, приближая его к виду декларативных языков.
 */

#include <stdio.h>

/**
 * @def PRINT(a)
 * @brief Макрос для мгновенного вывода целочисленного значения.
 *
 * Разворачивается в стандартный вызов функции printf() с автоматическим
 * добавлением символа переноса строки. Избавляет разработчика от ручного
 * написания спецификаторов формата.
 *
 * @param a Целочисленная переменная или константа (тип int), которую нужно напечатать.
 */
#define PRINT(a) printf("%d\n", a);

/**
 * @def LOOP(v, s, e)
 * @brief Макрос инициализации структурированного цикла со строгим шагом.
 *
 * Создает инкапсулированный заголовок цикла 'for'. Объявляет локальную
 * переменную-счетчик внутри области видимости цикла, что предотвращает
 * загрязнение глобального пространства имен. Итерация происходит с шагом +1.
 *
 * @param v Имя создаваемой управляющей переменной (счетчика).
 * @param s Начальное пороговое значение (инклюзивное).
 * @param e Конечное пороговое значение (инклюзивное).
 */
#define LOOP(v, s, e) for (int v = s; v <= e; v++) {

/**
 * @def ENDLOOP
 * @brief Макрос безопасного закрытия логического блока.
 *
 * Представляет собой закрывающую фигурную скобку. Используется исключительно
 * в синтаксической паре с макросом LOOP() для явного разграничения границ
 * итерационного процесса и обеспечения баланса скобок в исходном коде.
 */
#define ENDLOOP }

/**
 * @brief **execute_linear_range_counter** — Главная управляющая функция программы.
 *
 * Название функции отражает её суть: последовательное выполнение (execute)
 * инкрементирующего подсчета в заданном линейном диапазоне (linear range counter).
 * Функция демонстрирует практическое применение кастомных синтаксических
 * макросов для генерации последовательности чисел от 1 до 10 включительно.
 *
 * ### Алгоритм работы:
 * 1. Инициализируется итератор `counter` со значением 1.
 * 2. На каждом шаге значение `counter` передается в поток вывода.
 * 3. Происходит инкремент до достижения верхнего порога 10.
 * 4. Ресурсы освобождаются, управление возвращается операционной системе.
 *
 * @param args Количество аргументов, переданных через командную строку при запуске.
 * @param argv Массив указателей на строки, содержащие сами аргументы командной строки.
 *
 * @return **0** при успешном завершении работы программы.
 */
int main(int args, char **argv) {

    LOOP(counter, 1, 10)
        PRINT(counter)
    ENDLOOP

    return 0;
}




